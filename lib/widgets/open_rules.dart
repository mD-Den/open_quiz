import 'package:flutter/material.dart';

class OpenRules extends StatefulWidget {
  const OpenRules({Key? key}) : super(key: key);

  @override
  _OpenRulesState createState() => _OpenRulesState();
}

class _OpenRulesState extends State<OpenRules> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
